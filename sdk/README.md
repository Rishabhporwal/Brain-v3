# SDK — public/customer-facing SDKs (Data Platform / DX)

Nothing here yet. Planned packages (`browser`, `core`, `server`, `mobile`) are generated
from `contracts/openapi` + hand-written ergonomics once the public API stabilizes
(see Brain_Repository_Architecture_v2.md §13). Distinct from internal tracking SDKs
(`data-platform/first-party-data/tracking`, cataloged) and internal libs (`shared/`).
