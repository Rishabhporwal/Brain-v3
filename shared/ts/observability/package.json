{
  "name": "@brain/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Structured JSON logging + trace correlation. Every log line carries traceId + (where known) brandId; helpers redact PII. The spine for explaining any figure or action.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "vitest run" },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
