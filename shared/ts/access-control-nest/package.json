{
  "name": "@brain/access-control-nest",
  "version": "0.0.0",
  "private": true,
  "description": "NestJS adoption layer for @brain/access-control. AccessControlModule.forRoot() wires the AccessControl seam, operator IdentityService, BrandContextGuard, PermissionGuard, and the fail-closed exception filter — so a service adopts tenant isolation + RBAC with one import.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "vitest run" },
  "dependencies": {
    "@brain/access-control": "workspace:*",
    "pg": "^8.13.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "reflect-metadata": "^0.2.0"
  },
  "devDependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "reflect-metadata": "^0.2.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
