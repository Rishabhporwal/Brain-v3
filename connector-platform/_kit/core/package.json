{
  "name": "@brain/connector-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Shared connector framework — the contract + engines every connector composes (OAuth, webhook verify/dedupe, ingest records). Write once, reuse 100×.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
