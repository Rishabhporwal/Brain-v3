{
  "name": "@brain/raw-archiver",
  "version": "0.1.0",
  "private": true,
  "description": "Raw-as-received archive of every integration topic to S3/Iceberg-compatible object storage (BRD §10.4: the warehouse keeps raw data exactly as received).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "kafkajs": "^2.2.4",
    "kafkajs-snappy": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
