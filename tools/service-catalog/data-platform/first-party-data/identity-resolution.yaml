# Service manifest — machine-readable ownership / SLO / contract registry.
name: identity-resolution
owner_pod: cdp
phase: P1
language: nestjs-ts
summary: >-
  Deterministic key matching in-stream; merge/split; auditable (Neo4j overlay P6).
slos:
  availability: "99.9%"
  latency_p99_ms: 250
contracts:
  consumes: []          # e.g. [proto/commerce/metric.proto, events/avro/order.normalized.avsc]
  produces: []
data_owned: []          # tables/topics this service owns (no shared tables — Blueprint §2.14)
on_call: cdp
path: data-platform/first-party-data/identity-resolution
