<clickhouse>
  <!-- Listen on all interfaces so the host (and tools like DBeaver / the /play UI) can connect. -->
  <listen_host>0.0.0.0</listen_host>
  <!-- Allow Brain's custom query-scoped setting used by row policies (brand_id isolation). -->
  <custom_settings_prefixes>brain_</custom_settings_prefixes>
</clickhouse>
