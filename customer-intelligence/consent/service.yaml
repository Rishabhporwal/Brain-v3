# Service manifest — machine-readable ownership / SLO / contract registry.
name: consent
owner_pod: cdp
phase:
  P1 # was P3 — pixel tracking must be consent-gated from Phase 1 (BRD §10.2/§27.4;
  # DPDP/DLT/NCPR + WhatsApp opt-in). Lean at P1 (opt-in/out state per
  # customer-channel-purpose); withdrawal/erasure flows harden later.
language: nestjs-ts
summary: >-
  Channel/purpose/region consent, withdrawal, suppression; enforced on every send and
  stamped at capture by the tracking SDK.
slos:
  availability: '99.9%'
  latency_p99_ms: 250
contracts:
  consumes: [] # e.g. [proto/commerce/metric.proto, events/avro/order.normalized.avsc]
  produces: []
data_owned: [] # tables/topics this service owns (no shared tables — Blueprint §2.14)
on_call: cdp
